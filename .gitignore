.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Local, potentially ephemeral data and run artifacts.
/shm
/data/cache/
/runs/
/report.html
/.rig.toml
/.env*

# JAX compilation/profile artifacts.
/.jax_cache/
/profiles/

# Local config backups.
/.rig.toml.*

# Converted logs and the HuggingFace upload tree; the dataset is the record.
/logs/
/hf-dataset/

# Chart generator: it reads the local run archives, so it is only useful here.
/docs/reports/make_transfer_charts.py
